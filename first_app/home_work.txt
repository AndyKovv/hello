# Додати таблицю Post в ORM (створити об'єкт)

# Зміниті всі виклики до бази данних з голим SQL на об'єкти
в вашому проекті

# Зробити АPI для додавання постів в базу данних (Вадим)
# Зробити API для редагування постів.
# Зробити API для видалення постів.
# Зробити API для виведення постів по користувачу.
/api/v1/<user_id>/posts/ -> список постів юзера
/api/v1/<user_id>/posts/<post_is> -> Виводимо пост користувача

# 01.03.2023
# Зробити API для редагування профілю, а саме, номера телефону, ім'я та прізвища.
/api/v1/user-info/edit/<user_id> -> PUT.
 - Валідація чи профіль є власністю користувача.
 - Перевірити чи такий телефон існує.
 - Замнити данні користувача.

# Зробити API для редагування профілю, а саме зміну паролю.
/api/v1/user-info/change-password/ -> PUT. -> {
    "old_password": "fssdsds",
    "new_password": "nfewdwdw"
}
 -  Валідація старого паролю, якщо співпав, змінити на новий

# Alembic додати -> 
# https://flask-alembic.readthedocs.io/en/2.0.1/
